# Testing Checklist

- test status transitions across queued, running, and terminal states
- test timeline ordering after retries
- test approval state rendering inside the timeline
- test screen reader labels for tool status
- test long tool summaries on mobile layouts
