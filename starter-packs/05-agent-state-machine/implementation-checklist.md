# Implementation Checklist

- define a small, stable set of user-visible agent states
- keep backend orchestration phases mapped into frontend language
- avoid impossible state combinations in the UI
- track state transitions with timestamps
- expose recovery and approval states explicitly
