# Testing Checklist

- test unresolved checkpoint rendering
- test resolution state changes
- test persistence across reloads if supported
- test assistive labels for checkpoint purpose
- test fallback behavior when no responder is available
