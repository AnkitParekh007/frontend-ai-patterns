# Testing Checklist

- test redaction behavior for sensitive fields
- test route-specific payload shaping
- test snapshot version compatibility if used
- test empty optional fields
- test that hidden fields do not leak into transport
