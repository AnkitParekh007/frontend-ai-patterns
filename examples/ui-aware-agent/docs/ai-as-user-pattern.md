# AI As User Pattern

The agent is modeled as a visible participant in the interface:

- It reads safe page context.
- It suggests actions instead of silently executing.
- It asks for approval before sensitive workflow changes.
- It shows execution and recovery state.

This pattern is useful for enterprise applications where automation must remain understandable and auditable.
