# Recruiter Notes

This repo is a public proof demo for UI-aware agent interfaces in Angular. It uses mock data only and does not claim production automation.

Look for the context inspector, suggested actions, approval flow, execution timeline, and recovery panel.
